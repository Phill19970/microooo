spring:
  application:
    name: user-service

  config:
    uri: http://config-server:8888

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://registry-service:8761/eureka/